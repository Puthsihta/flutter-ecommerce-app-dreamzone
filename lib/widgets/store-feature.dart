import 'package:flutter/material.dart';

class StoreFeature extends StatelessWidget {
  const StoreFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
