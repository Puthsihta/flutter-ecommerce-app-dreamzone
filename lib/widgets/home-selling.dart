import 'package:flutter/material.dart';

class HomeSelling extends StatelessWidget {
  const HomeSelling({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
